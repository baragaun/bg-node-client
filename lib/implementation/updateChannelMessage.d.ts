import { ChannelMessage } from '../types/models/ChannelMessage.js';
declare const updateChannelMessage: (channelMessage: ChannelMessage) => Promise<ChannelMessage>;
export default updateChannelMessage;
