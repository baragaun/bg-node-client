import { ChannelMessage } from '../../types/models/ChannelMessage.js';
declare const formatChannelMessageForDb: (obj: Partial<ChannelMessage>) => Partial<ChannelMessage>;
export default formatChannelMessageForDb;
