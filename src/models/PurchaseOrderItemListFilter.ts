import { BaseListFilter } from './BaseListFilter.js';

export class PurchaseOrderItemListFilter extends BaseListFilter {

  constructor(attributes?: Partial<PurchaseOrderItemListFilter>) {
    super(attributes);
  }
}
