import { BaseListFilter } from './BaseListFilter.js';

export class ShoppingCartItemListFilter extends BaseListFilter {

  constructor(attributes?: Partial<ShoppingCartItemListFilter>) {
    super(attributes);
  }
}
